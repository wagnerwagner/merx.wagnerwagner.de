type: pages
parent: site.find("orders")
template: order
sortBy: num desc
text: "#{{ page.invoiceNumber }} – {{ page.name }} ({{ page.email }})"
info: "{{ page.formattedSum }} – {{ page.invoiceDate.toDate('Y-m-d') }}"
